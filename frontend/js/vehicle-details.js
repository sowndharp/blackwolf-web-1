const details = document.getElementById("vehicleDetails");
const vehicleId = new URLSearchParams(window.location.search).get("id");
fetch(`/api/vehicles/${vehicleId || 1}`).then((response) => { if (!response.ok) throw new Error(); return response.json(); }).then(({ vehicle }) => { details.innerHTML = `<div class="detail-layout"><img src="${vehicle.image}" alt="${vehicle.model}"><div class="detail-copy"><p class="eyebrow">${vehicle.series} / ${vehicle.year}</p><h1>${vehicle.model}</h1><p class="price">${formatPrice(vehicle.price)}</p><p class="muted">${vehicle.description}</p><div class="specs"><div><small>Engine</small>${vehicle.engine}</div><div><small>Power</small>${vehicle.power}</div><div><small>Transmission</small>${vehicle.transmission}</div><div><small>Fuel</small>${vehicle.fuel}</div></div><a class="btn btn-primary" href="/enquiry.html?vehicleId=${vehicle.id}">Enquire about this model</a></div></div>`; }).catch(() => { details.innerHTML = "<p class='loading'>Vehicle not found.</p>"; });