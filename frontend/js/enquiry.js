const enquiryForm = document.getElementById("enquiryForm");
const vehicleSelect = document.getElementById("vehicleId");
fetch("/api/vehicles").then((response) => response.json()).then(({ vehicles }) => { vehicles.forEach((vehicle) => { vehicleSelect.insertAdjacentHTML("beforeend", `<option value="${vehicle.id}">${vehicle.model}</option>`); }); vehicleSelect.value = new URLSearchParams(window.location.search).get("vehicleId") || ""; });
enquiryForm.addEventListener("submit", async (event) => { event.preventDefault(); const message = document.getElementById("formMessage"); const payload = { name: document.getElementById("name").value, email: document.getElementById("email").value, phone: document.getElementById("phone").value, vehicleId: vehicleSelect.value || null, message: document.getElementById("message").value }; try { const response = await fetch("/api/enquiries", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }); const data = await response.json(); if (!response.ok) throw new Error(data.message); message.textContent = data.message; enquiryForm.reset(); } catch (error) { message.textContent = error.message || "Unable to submit enquiry."; } });